---
// ===== ArticleTimeline.astro — Left sidebar: articles grouped by year =====
import { articles } from "../data/articles";
import { getLabel } from "../data/tags";

export interface Props {
  activePath?: string;
}

const { activePath } = Astro.props;

// Group by year
const byYear = new Map<string, typeof articles>();
articles.forEach((a) => {
  const year = a.date.slice(0, 4);
  if (!byYear.has(year)) byYear.set(year, []);
  byYear.get(year)!.push(a);
});

// Sort years descending
const years = Array.from(byYear.keys()).sort((a, b) => Number(b) - Number(a));
---

  <aside class="w-56 shrink-0 hidden lg:block">
  <div class="sticky top-20 pr-6 max-h-[calc(100vh-6rem)] overflow-y-auto">
    <h4 class="text-[10px] font-semibold uppercase tracking-widest text-zinc-400 dark:text-slate-500 mb-4">
      全部笔记
    </h4>

    <nav class="space-y-5">
      {
        years.map((year) => (
          <div>
            <div class="flex items-center gap-2 mb-2">
              <span class="w-1.5 h-1.5 rounded-full bg-zinc-300 dark:bg-slate-600" />
              <span class="text-xs font-semibold text-zinc-500 dark:text-slate-400">{year}</span>
            </div>
            <ul class="space-y-0.5">
              {byYear.get(year)!.map((a) => {
                const isActive = activePath?.trim() === a.href?.trim();
                return (
                  <li>
                    <a
                      href={a.href}
                      class={`
                        block text-sm py-1 pl-4 border-l transition-all duration-200 rounded-r-lg
                        ${isActive
                          ? "border-blue-500 bg-blue-50 dark:bg-blue-900/20 text-blue-700 dark:text-blue-300 font-medium"
                          : "border-zinc-200 dark:border-slate-800 text-zinc-500 dark:text-slate-400 hover:border-zinc-400 dark:hover:border-slate-600 hover:text-zinc-700 dark:hover:text-slate-300"
                        }
                      `}
                    >
                      {a.title}
                    </a>
                  </li>
                );
              })}
            </ul>
          </div>
        ))
      }
    </nav>
  </div>
</aside>
