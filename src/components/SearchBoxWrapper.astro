---
// ===== SearchBoxWrapper.astro =====
import SearchBox from "./SearchBox.tsx";
export interface Props {
  variant?: "button" | "inline";
}
const { variant = "button" } = Astro.props;
---

<SearchBox client:load variant={variant} />
